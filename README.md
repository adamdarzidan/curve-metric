# Instructions
To run program: `python3 __main__.py`
Upon running, you must either:
1. Load an existing model from `weights/` or
2. Train a new model by using the `TRAIN` command and provide it with a path to the dataset being trained on.

Once completed, the UI should say `MODEL: {filepath}`.

   
